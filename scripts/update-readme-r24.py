#!/usr/bin/env python3
# r24: تحديث README — وصف اللاندينج العالمية الجديدة وبوابة الدخول
import io

PATH = "/home/z/my-project/README.md"
with io.open(PATH, "r", encoding="utf-8") as f:
    src = f.read()

# 1) جدول صفحات الموقع العام: صف الرئيسية + صف الدخول
old_home_row = "| 🏠 الرئيسية | `#/` | هيرو ببطاقة فاتورة عائمة + **إحصاءات حيّة** من `GET /api/site/stats` + 6 بطاقات مزايا + شريط الشركات (من `/api/companies`) + لمحة عن المؤسس |"
new_home_row = "| 🏠 الرئيسية | `#/` | شريط جاهزية (196 دولة · 28 لغة · أي عملة) + بطل مقسوم (سلايدر «مرحباً بالعالم» بـ77 لغة + عنوان التموضع العالمي + موكاب لوحة حية: KPIs/رسم إيراد/فاتورة/نافذة GarfiX AI) + **شريط تحكم عالمي تفاعلي** (لغة/اتجاه/عملة/ضريبة/دولة — يُغذي أرقام الموكاب وعرض الـAI والتسعير) + 6 كروت وحدات بأيقونات lucide + عرض GarfiX AI (كتابة تلقائية → كارت مراجعة → الموافقة والإنشاء) + قبل/بعد (واتساب → محاسبة نظيفة) + باقة واحدة 10$/شركة/شهر مع عدّاد «مجاناً لأول 100 شركة» + ثقة المؤسس + **إحصاءات حيّة** وشريط الشركات + دعوة أخيرة |"
assert old_home_row in src, "home row not found"
src = src.replace(old_home_row, new_home_row)

old_login_row = "| 🔐 الدخول | `#/login` | شاشة الدخول نفسها داخل إطار مع رابط عودة للموقع |"
new_login_row = "| 🔐 بوابة الدخول | `#/login` | **بوابة SaaS (r24):** نموذج الدخول/التسجيل/الاستعادة + معاينة حية للوحة بعملة قابلة للتبديل + نقاط ثقة + لغة/عملة/سمة أعلى + شريط «مجاني لأول 100 شركة» أسفل — و`#/login?mode=register` يفتح تبويب التسجيل مباشرة (وجهة زر «ابدأ مجاناً») |"
assert old_login_row in src, "login row not found"
src = src.replace(old_login_row, new_login_row)

# 2) سطر «التصميم» في قسم الموقع العام (متحقَّق بتشكيل مختلف — نمط regex)
import re
m = re.search(r"- \*\*التصميم:\*\* هوية كحلي \+ ذهبي.*?390px بلا تمرير أفقي \([^)]*\)", src)
assert m, "design line not found"
new_design = "- **نظام التصميم (r24):** خط Inter + Cairo/Tajawal (سقوط تلقائي للعربية) وأرقام جدولية للماليات · بطاقات بحواف 20px وظلال ناعمة وزجاج خفيف في البطل · أيقونات lucide-react بدل الإيموجي في كل الواجهة العامة · شعار «G» ذهبي · هوية كحلي عميق + ذهبي مع لمسات أزرق ملكي للـAI · فوتر ملتصق بأسفل الشاشة — متجاوب حتى 390px بلا تمرير أفقي (متحقَّق)"
src = src[:m.start()] + new_design + src[m.end():]

# 3) بانر آخر تحقق شامل r23 → r24
old_banner = "> ✅ **آخر تحقق شامل (r23 — سبتمبر 2026):** سلايدر «مرحباً بالعالم» بـ77 لغة في البطل + محافظات ومقاطعات 195 دولة في بطاقة العميل (فلسطين بلا إسرائيل) + تصدير/استيراد العملاء بالأعمدة الجديدة — مراجعة E2E بالمتصفح عربي/إنجليزي، جوال 393px بلا overflow، lint نظيف. التفاصيل في [منجز حديثاً](#-منجز-حديثاً)."
new_banner = "> ✅ **آخر تحقق شامل (r24 — سبتمبر 2026):** لاندينج **Global AI Business OS** عالمية فاخرة (موكاب لوحة حي بعملة قابلة للتبديل + شريط تحكم عالمي تفاعلي + عرض AI بمراجعة قبل التنفيذ + باقة 10$ واحدة) وبوابة دخول SaaS — E2E عربي/إنجليزي/جوال 390px بلا overflow، دخول فعلي وتطبيق سليمان، lint نظيف. التفاصيل في [منجز حديثاً](#-منجز-حديثاً)."
assert old_banner in src, "banner not found"
src = src.replace(old_banner, new_banner)

# 4) مقدمة القسم: إشارة r24
old_intro = "منذ r13 يفتح الزائر على **موقع عام متعدد الصفحات** قبل أي شاشة دخول — بتوجيه hash داخل مسار `/` الواحد (SPA واحدة بلا إعادة تحميل ولا مسارات خادم إضافية):"
new_intro = "منذ r13 يفتح الزائر على **موقع عام متعدد الصفحات** قبل أي شاشة دخول — بتوجيه hash داخل مسار `/` الواحد (SPA واحدة بلا إعادة تحميل ولا مسارات خادم إضافية)، ومنذ **r24** صار الموقع **لاندينج «GarfiX — AI Business OS» عالمية فاخرة** تبيع الاشتراك من أول شاشة:"
assert old_intro in src, "intro not found"
src = src.replace(old_intro, new_intro)

with io.open(PATH, "w", encoding="utf-8") as f:
    f.write(src)
print("README.md updated (4 sections)")
